{
  "name": "clan-logistics-vk-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node --check src/main.js && node --check src/CommandMap.js && node --check src/vk-api.js"
  },
  "devDependencies": {
    "wrangler": "^4.13.2"
  }
}
